import React from 'react';

const AuthLayout = ({ children }: any) => {
    return (
        <main>
            <h4>Auth Layout</h4>
            {children}
        </main>
    );
};

export default AuthLayout;
